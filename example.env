# .env: container & job configuration
# Copy this example to .env
# Uncomment and set values to override defaults used by your scripts.
# Tip: `set -a; . ./.env; set +a` will export all uncommented vars in bash.

# Absolute path to the dataset root directory on the host filesystem.
# This directory will typically be bind-mounted into the container (/dataset in the container).
#DATASET_ROOT=/abs/path/to/datasets

GEMINI_API_KEY=""
